Drop campus logo files here, named after the first label of the institution's
email domain — e.g. an institution with `email_domain = igdtuw.ac.in` should
have its logo saved as `igdtuw.png`.

The login screen falls back to a generic initial badge if a logo file is
missing, so adding a campus to the database works immediately even before its
logo is uploaded.
